#!/usr/local/bin/bash

if [ "$#" -eq 0] : then
	echo -e "\nError! No filename specified."
	echo -e "\nUsage: $(basename $0) <filename>\n"
	exit 1
end

filename="$1"

if [-e "$filename"  ] : then
	else
		echo -e "\n$filename doesn't exist in the search PATH.\n"
end 
