JAVASCRIPT
// Set something to the middle of the page

foo.x = Math.round(this._width / 2);
foo.y = Math.round(this._height / 2);

GIT
// Stop tracking changes to a file so that it is ignored by git add -a
git update-index --assume-unchanged <file>
// Start tracking again
git update-index --no-assume-unchanged <file>

// Amend a commit
git commit --amend //this only adds to previous commit not, current uncommitted chnages

git log
git show head
git add . or <file>
git diff
git reset -hard or -soft
git shortlog -sne //shorter, numerical by commits, email

GREP
// Check to see if two files are equal
// Flags 
// -F --fixed-strings; -x -select only that exactly match the whole line.
// -v invert, so we'll get everything that doesn't match exactly
// -f file, obtain patterns from FILE one per line. 
grep -Fxvf file1 file2

PR
// print command, I used it to print directory output in more than one column
ls ../* | pr -2 -w 200 | less
*/ 
// Note that the */ or whatever is just there to stop the formatting from fucking up

NODE
// There are good notes and code samples in the psight folder
/Users/casa/Documents/pluralsight/node

SOCKET.IO

io.emit
io.on('connection', function(socket){
	console.log('a user connected');
	socket.on('disconnect', function(){
		console.log('user disconnected');
	});
});

SCP
scp your_username@remotehost.edu:foobar.txt /some/local/directory

NETWORKING
// Get current ip from cli

curl icanhazip.com
